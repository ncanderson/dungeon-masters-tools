# Move default index.html location

#spring.resources.static-locations=/resources/static/**
#spring.thymeleaf.enabled=false

# Datasource

# Local

# World Builder
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5433/world_builder
spring.datasource.platform=postgresql
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.initialize=false

# Monster Builder
spring.monsterBuilder.driver-class-name=org.postgresql.Driver
spring.monsterBuilder.url=jdbc:postgresql://localhost:5433/monster_builder
spring.monsterBuilder.platform=postgresql
spring.monsterBuilder.username=postgres
spring.monsterBuilder.password=postgres
spring.monsterBuilder.initialize=false

# Hibernate

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

